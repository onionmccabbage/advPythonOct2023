this might be a very large amount of text that we need to persist into a file without blocking the main thread
this might be a very large amount of text that we need to persist into a file without blocking the main thread

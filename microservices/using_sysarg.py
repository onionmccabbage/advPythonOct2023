import sys

# we can inject values into any python module when we run it
# these are called system argument variables
print(sys.argv) # there will ALWAYS be one sys.argv - the current running module path